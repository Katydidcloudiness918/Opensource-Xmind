# This app runs entirely in your browser. No account and no required API keys.
# Copy to .env if you want to override the displayed name.
# Never put real secrets in committed files.

VITE_APP_NAME=Local Mind Map

# Optional. Reserved for a future self-hosted backup endpoint.
# The editor stays fully usable when this is empty (local IndexedDB only).
VITE_BACKUP_URL=
